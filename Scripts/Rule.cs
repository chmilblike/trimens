using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rule : MonoBehaviour
{

    static public bool _isValidated;
    static public string _returnRuleAction;
    //static public List<string> _returnRulesActions; // Checker si lorsqu'on le modifie depuis une classe filles les autre non pas la modif

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
